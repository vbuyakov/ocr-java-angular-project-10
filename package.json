{
  "name": "ycyw-root",
  "private": true,
  "description": "Root package for semantic-release only (see .github/workflows/release.yml)",
  "devDependencies": {
    "@semantic-release/changelog": "^6.0.3",
    "@semantic-release/git": "^10.0.1",
    "semantic-release": "^24.2.3"
  }
}
